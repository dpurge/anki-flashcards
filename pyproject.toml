[project]
name = "anki-flashcards"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "D. Purge", email = "david.purge@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "anki>=25.9.2",
    "pydantic>=2.12.5",
]

[project.scripts]
anki-build = "anki_flashcards:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
